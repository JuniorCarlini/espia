// Copy this file to `wifi_credentials.h` (gitignored — never commit real
// credentials) and fill in a real network. This is a build-step-3
// placeholder only: real WiFi setup will be the captive portal ADR 0015
// describes, not a hardcoded network.

#pragma once

#define ESPIA_WIFI_SSID "your-network-name"
#define ESPIA_WIFI_PASSWORD "your-network-password"
